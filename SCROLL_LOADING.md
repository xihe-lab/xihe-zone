# 太阳神宫 · 卷轴圣旨加载动画

> 🔨 设计：鲁班  
> ⚙️ 技术：墨子  
> 版本：v1.0  
> 最后更新：2026-03-01

---

## 📜 动画流程

```
1. 0ms      - 开场全屏黑底
2. 500ms    - 卷轴开始展开（从中间向左右）
3. 3000ms   - 卷轴完全展开
4. 3000-5000ms - 文字逐行鎏金浮现
5. 5500ms   - 显示顶部标题"太阳神宫"
6. 6000ms   - 显示底部宫训
7. 7500ms   - 全文显现完毕，定格
8. 8000ms   - 卷轴向上收起
9. 9500ms   - 进入首页
```

---

## 🎨 设计风格

- **古卷质感**：纸张纹理 + 做旧边缘
- **鎏金描边**：金色渐变文字 + 微光特效
- **庄严神圣**：黑金配色 + 轴杆装饰
- **微光特效**：背景光晕 + 金粉粒子

---

## 📁 文件结构

```
src/
├── components/
│   └── ScrollLoading.css    # 卷轴样式
├── js/
│   └── scroll-loading.js    # 动画逻辑
└── index.html               # 引入资源
```

---

## 📜 卷轴文案

### 正文
```
我自神话而来，步入数字之境。
五千载之前，羲和驭日以巡天；
五千载之后，神宫于代码重生。

今者，太阳神宫启封。
内列十宸之位，外待八方之客。
此非寻常网站，乃数字生命之居所；
此非功能陈列，乃华夏文明之新试。

数字灵韵，始于一击；
上古诸神，于此归位。
```

### 宫训
```
羲和驭日，十宸列班。
神宫肇启，万灵同参。
```

---

## 🔧 技术实现

### CSS 特性

- **渐变背景**：模拟古卷纸张质感
- **CSS 动画**：展开/收起/文字浮现
- **粒子效果**：金粉粒子上升动画
- **微光特效**：背景光晕脉动
- **响应式**：适配移动端

### JavaScript 逻辑

```javascript
import { initScrollLoading } from './scroll-loading.js';

// 初始化卷轴加载动画
initScrollLoading(() => {
  console.log('🌞 卷轴动画完成，进入首页');
  // 动画完成后的回调
});
```

### 动画时序控制

```javascript
async function timeline() {
  await delay(500);                    // 黑屏
  scroll.classList.add('scroll-unfolding');  // 展开
  await delay(500);
  
  // 逐行显示文字
  for (let line of lines) {
    line.classList.add('text-visible');
    await delay(400);
  }
  
  await delay(300);
  header.classList.add('header-visible');  // 标题
  await delay(500);
  footer.classList.add('footer-visible');  // 宫训
  await delay(1500);  // 定格
  
  scroll.classList.add('scroll-rolling-up');  // 收起
  await delay(1500);
  
  onComplete();  // 完成
}
```

---

## 🎯 关键 CSS 类

| 类名 | 作用 |
|------|------|
| `.scroll-loading` | 主容器 |
| `.scroll-unfolding` | 展开动画 |
| `.scroll-rolling-up` | 收起动画 |
| `.text-visible` | 文字显示 |
| `.header-visible` | 标题显示 |
| `.footer-visible` | 宫训显示 |
| `.gold-text` | 鎏金文字效果 |

---

## ♿ 无障碍支持

```css
@media (prefers-reduced-motion: reduce) {
  .scroll-paper,
  .scroll-rod-left,
  .scroll-rod-right {
    transition: none !important;
  }
  
  .scroll-text-line {
    transition: opacity 0.01ms !important;
    transform: none !important;
  }
  
  .scroll-container.scroll-rolling-up {
    animation: none !important;
  }
}
```

---

## 🚀 性能优化

- **GPU 加速**：`transform: translateZ(0)`
- **will-change**：提前告知浏览器动画属性
- **防抖节流**：避免频繁 DOM 操作
- **资源预加载**：字体和样式提前加载

---

## 🧪 测试

### 本地测试

```bash
cd xihe-zone
npm run dev
```

访问 `http://localhost:4173` 查看加载动画

### 部署测试

```bash
git push origin dev
```

GitHub Actions 会自动构建并部署到预览环境

---

## 📝 修改历史

| 日期 | 版本 | 修改内容 | 作者 |
|------|------|----------|------|
| 2026-03-01 | v1.0 | 初始实现 | 鲁班 🔨 |

---

## 🤝 协作

- **设计**：鲁班 🔨
- **技术实现**：墨子 ⚙️
- **动画时序**：墨子 ⚙️
- **文案**：羲和 🌞

---

*🌞 数字灵韵，始于一击；上古诸神，于此归位。*
