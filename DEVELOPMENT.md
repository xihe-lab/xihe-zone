# 羲和实验室 - 开发指南

> 🏛️ 墨子 ⚙️ 技术实现文档

---

## 📋 目录

1. [快速开始](#快速开始)
2. [项目结构](#项目结构)
3. [开发工作流](#开发工作流)
4. [组件使用](#组件使用)
5. [样式定制](#样式定制)
6. [性能优化](#性能优化)
7. [部署上线](#部署上线)
8. [常见问题](#常见问题)

---

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 8.0.0
- 现代浏览器 (Chrome, Firefox, Safari, Edge)

### 安装依赖

```bash
# 进入项目目录
cd xihe-zone

# 安装依赖
npm install
```

### 启动开发服务器

```bash
# 启动本地开发服务器 (热重载)
npm run dev

# 访问 http://localhost:5173
```

### 构建生产版本

```bash
# 构建静态文件
npm run build

# 预览构建结果
npm run preview
```

---

## 📁 项目结构

```
xihe-zone/
├── .vitepress/              # VitePress 配置
│   └── config.js           # 主配置文件
├── public/                  # 静态资源
│   └── logo.svg            # 网站 Logo
├── src/                     # 源代码
│   ├── css/                # 样式文件
│   │   └── main.css        # 主样式 (Tailwind + 自定义)
│   ├── js/                 # JavaScript 文件
│   │   ├── main.js         # 主入口文件
│   │   ├── animations.js   # 动画模块
│   │   └── components.js   # 组件模块
│   └── index.html          # HTML 模板
├── design/                  # 设计资源
│   ├── tech-specs.md       # 技术规范
│   └── assets/             # 设计稿
├── index.md                # Markdown 版本主页 (兼容 VitePress)
├── package.json            # 项目配置
├── tailwind.config.js      # Tailwind CSS 配置
├── postcss.config.js       # PostCSS 配置
└── README.md               # 项目说明
```

---

## 🔧 开发工作流

### 1. 等待设计规范

当前状态：⏳ 等待鲁班 🔨 输出详细设计规范

需要确认的内容：

- 色彩方案（主色、辅助色、渐变色）
- 字体选择（标题字体、正文字体）
- 布局设计（页面结构、间距规范）
- 视觉元素（Logo 细节、图标风格、装饰图案）
- 交互动效（页面切换、按钮交互、滚动效果）

### 2. 修改页面内容

**方式一：编辑 JavaScript 组件**

```javascript
// src/js/main.js 中的 renderHero() 函数
function renderHero() {
  return `
    <section class="hero-section" id="home">
      <!-- 修改内容 -->
    </section>
  `;
}
```

**方式二：使用 VitePress Markdown**

```markdown
## <!-- index.md -->

layout: home title: 羲和实验室

---

# 欢迎来到羲和实验室

内容...
```

### 3. 添加新组件

```javascript
// src/js/components.js
export function createNewComponent() {
  return {
    template: `
      <div class="new-component">
        <!-- 组件内容 -->
      </div>
    `,
    props: {
      // 组件属性
    },
  };
}
```

### 4. 添加动画效果

```javascript
// src/js/animations.js
export function addCustomAnimation() {
  // 自定义动画逻辑
}
```

---

## 🎨 样式定制

### Tailwind CSS 配置

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        'xihe-gold': {
          50: '#fffbeb',
          500: '#f59e0b', // 主金色
          900: '#78350f',
        },
      },
    },
  },
};
```

### 自定义 CSS 变量

```css
/* src/css/main.css */
:root {
  --xihe-gold-primary: #f59e0b;
  --sun-gradient: linear-gradient(135deg, #f59e0b 0%, #f97316 50%, #dc2626 100%);
}
```

### 添加新颜色

在 `tailwind.config.js` 中添加：

```javascript
colors: {
  'xihe-new': {
    100: '...',
    500: '...', // 主色
    900: '...',
  }
}
```

使用：`class="text-xihe-new-500 bg-xihe-new-100"`

---

## 🧩 组件使用

### 内置组件

| 组件名           | 说明         | 使用位置   |
| ---------------- | ------------ | ---------- |
| `Hero`           | 首页英雄区域 | 页面顶部   |
| `FeatureCard`    | 特性展示卡片 | 核心能力区 |
| `ContentSection` | 内容区块     | 各章节     |
| `ProjectCard`    | 项目展示卡片 | 项目区     |
| `ArticleCard`    | 文章卡片     | 文章区     |
| `Footer`         | 页脚         | 页面底部   |

### 使用示例

```javascript
import { renderComponent } from './components.js';

// 渲染特性卡片
const html = renderComponent('featureCard', {
  icon: '🤖',
  title: 'AI 助手',
  details: '24 小时在线',
});
```

---

## ⚡ 性能优化

### 已实现的优化

- ✅ 代码分割 (Code Splitting)
- ✅ 懒加载 (Lazy Loading)
- ✅ 图片优化 (WebP 格式)
- ✅ CSS 压缩
- ✅ JavaScript 压缩
- ✅ 防抖/节流滚动事件
- ✅ Intersection Observer 动画
- ✅ 预加载关键资源

### Lighthouse 目标

| 指标           | 目标值 | 当前状态  |
| -------------- | ------ | --------- |
| Performance    | > 90   | ⏳ 待测试 |
| Accessibility  | > 90   | ⏳ 待测试 |
| Best Practices | > 90   | ⏳ 待测试 |
| SEO            | > 90   | ⏳ 待测试 |

### 优化建议

1. **图片优化**

   ```html
   <img src="image.webp" alt="..." loading="lazy" />
   ```

2. **字体优化**

   ```html
   <link rel="preconnect" href="https://fonts.googleapis.com" />
   ```

3. **代码分割**
   ```javascript
   // Vite 自动处理
   ```

---

## 🚀 部署上线

### 方式一：Gitee Pages

```bash
# 构建
npm run build

# 推送到 Gitee
git add .
git commit -m "build: 生产版本"
git push origin main

# 在 Gitee 仓库设置中启用 Pages 服务
# 选择 main 分支
# 绑定自定义域名 xihe.zone
```

### 方式二：Vercel

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel

# 按提示操作
```

### 方式三：Netlify

```bash
# 构建命令
npm run build

# 发布目录
dist/

# 在 Netlify 控制台配置
```

---

## ❓ 常见问题

### Q1: 开发服务器无法启动

**解决方案：**

```bash
# 删除 node_modules 和 lock 文件
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 重启
npm run dev
```

### Q2: 样式不生效

**检查清单：**

- [ ] Tailwind CSS 是否正确配置
- [ ] CSS 文件是否正确引入
- [ ] 浏览器缓存是否清除

### Q3: 动画效果卡顿

**优化方案：**

- 减少同时进行的动画数量
- 使用 CSS transform 代替 position
- 使用 requestAnimationFrame

### Q4: 移动端显示异常

**调试步骤：**

1. 使用浏览器开发者工具切换到移动模式
2. 检查断点设置是否正确
3. 测试不同尺寸设备

---

## 📞 技术支持

**负责人：** 墨子 ⚙️  
**联系方式：** xihe@xihe.zone  
**文档版本：** 1.0.0  
**最后更新：** 2026-02-28

---

## 🔄 待办事项

### 等待鲁班设计规范

- [ ] 确认色彩方案
- [ ] 确认字体选择
- [ ] 确认布局设计
- [ ] 确认视觉元素
- [ ] 确认交互动效

### 技术实现

- [x] 项目初始化
- [x] 基础架构搭建
- [x] 核心组件开发
- [x] 动画系统实现
- [ ] 根据设计规范调整样式
- [ ] 性能测试与优化
- [ ] 兼容性测试
- [ ] 部署上线

---

<p align="center">Made with 🌞 by 墨子 ⚙️</p>
