# 代码审查流程 · 羲和实验室

> 📖 白泽守护 · 代码质量把关

---

## 📋 目录

1. [审查流程](#审查流程)
2. [审查清单](#审查清单)
3. [审查报告模板](#审查报告模板)
4. [代码规范](#代码规范)
5. [自动化检查](#自动化检查)

---

## 审查流程

### 工作流程图

```
墨子 ⚙️                    白泽 📖
   │                         │
   │  提交代码到 dev          │
   │────────────────────────>│
   │                         │
   │                         │ 代码审查
   │                         │ - 代码质量
   │                         │ - 性能检查
   │                         │ - 安全审核
   │                         │
   │<────────────────────────│
   │  审查报告 + 修改建议     │
   │                         │
   │  修改后再次提交          │
   │────────────────────────>│
   │                         │
   │                         │ 审核通过
   │<────────────────────────│
   │                         │
   │  合并到 main 分支        │
   │  自动部署               │
```

### 审查步骤

1. **提交代码** - 墨子将代码提交到 `dev` 分支
2. **自动检查** - GitHub Actions 运行 ESLint、Stylelint 等工具
3. **人工审查** - 白泽进行代码质量、性能、安全审查
4. **反馈修改** - 输出审查报告，提出修改建议
5. **再次审查** - 修改后重新提交审查
6. **合并部署** - 审核通过后合并到 `main` 分支并自动部署

---

## 审查清单

### ✅ HTML 审查清单

- [ ] 语义化标签使用正确（`<header>`, `<main>`, `<footer>`, `<article>`, `<section>`）
- [ ] `lang` 属性设置正确
- [ ] `meta` 标签完整（`charset`, `viewport`, `description`）
- [ ] 图片有 `alt` 属性
- [ ] 表单元素有对应的 `<label>`
- [ ] 无废弃标签（如 `<font>`, `<center>`）
- [ ] 无障碍性检查（ARIA 属性、键盘导航）
- [ ] 响应式视口配置
- [ ] 关键资源预加载（`preconnect`, `preload`）

### ✅ CSS 审查清单

- [ ] 使用 CSS 变量统一管理颜色、字体、间距
- [ ] 无硬编码的魔法数字
- [ ] 媒体查询断点合理（移动优先）
- [ ] 无 `!important` 滥用
- [ ] 选择器特异性合理（避免过深嵌套）
- [ ] 动画有 `prefers-reduced-motion` 支持
- [ ] 关键帧动画命名清晰
- [ ] 无未使用的 CSS 规则
- [ ] 颜色对比度符合 WCAG 2.1 AA 标准

### ✅ JavaScript 审查清单

- [ ] 使用 `const`/`let`，避免 `var`
- [ ] 函数命名清晰（动词开头）
- [ ] 变量命名有意义（避免 `a`, `b`, `tmp`）
- [ ] 错误处理完善（`try-catch`）
- [ ] 无全局变量污染
- [ ] 事件监听器正确清理（防止内存泄漏）
- [ ] 异步操作使用 `Promise` 或 `async/await`
- [ ] 数组/对象操作使用现代 API（`map`, `filter`, `reduce`）
- [ ] 防抖/节流用于高频事件（`scroll`, `resize`）
- [ ] 使用 `requestAnimationFrame` 优化动画
- [ ] 被动事件监听器（`passive: true`）用于滚动优化

### ✅ 性能审查清单

- [ ] 图片使用现代格式（WebP/AVIF）+ 懒加载
- [ ] 关键 CSS 内联，非关键 CSS 异步加载
- [ ] JavaScript 代码分割（按需加载）
- [ ] 第三方资源使用 `preconnect`
- [ ] 避免布局抖动（批量 DOM 操作）
- [ ] 使用 `IntersectionObserver` 替代滚动监听
- [ ] 长列表使用虚拟滚动
- [ ] Lighthouse 性能分数 ≥ 90
- [ ] 首次内容绘制（FCP）< 1.5s
- [ ] 最大内容绘制（LCP）< 2.5s

### ✅ 安全审查清单

- [ ] 无 `innerHTML` 直接插入用户输入（防 XSS）
- [ ] 外部链接使用 `rel="noopener noreferrer"`
- [ ] CSP（内容安全策略）配置
- [ ] 敏感信息不硬编码在代码中
- [ ] 表单提交有 CSRF 防护
- [ ] 输入验证（前端 + 后端）
- [ ] 错误信息不泄露敏感数据
- [ ] HTTPS 强制使用
- [ ] 依赖包无已知漏洞（`npm audit`）

### ✅ 可维护性审查清单

- [ ] 代码有清晰的注释（为什么，而不是做什么）
- [ ] 函数单一职责（不超过 50 行）
- [ ] 文件结构清晰（按功能模块组织）
- [ ] 一致的代码风格（缩进、命名、空格）
- [ ] Git 提交信息规范（动词开头，简洁描述）
- [ ] 有 README 和文档
- [ ] 复杂逻辑有流程图或说明
- [ ] 配置与环境分离

### ✅ 响应式设计审查清单

- [ ] 移动优先策略
- [ ] 断点设置合理（320px, 768px, 1024px, 1440px）
- [ ] 图片响应式（`srcset`, `sizes`）
- [ ] 字体使用 `clamp()` 或 `vw` 单位
- [ ] 触摸目标 ≥ 44px
- [ ] 横向滚动已禁用（除非必要）
- [ ] 测试主流设备（iOS Safari, Chrome, Firefox）

### ✅ 浏览器兼容性审查清单

- [ ] 支持 Chrome 最新 2 个版本
- [ ] 支持 Firefox 最新 2 个版本
- [ ] 支持 Safari 最新 2 个版本
- [ ] 支持 Edge 最新 2 个版本
- [ ] 使用 Autoprefixer 自动添加厂商前缀
- [ ] 渐进增强策略（基础功能全浏览器可用）
- [ ] 特性检测替代浏览器检测

---

## 审查报告模板

```markdown
# 代码审查报告

**审查人：** 白泽 📖  
**审查日期：** YYYY-MM-DD  
**审查分支：** dev  
**提交人：** 墨子 ⚙️  
**审查范围：** [文件/模块列表]

---

## 📊 总体评价

**评分：** ⭐⭐⭐⭐☆ (4/5)  
**状态：** ✅ 通过 / ⚠️ 需修改 / ❌ 拒绝

---

## ✅ 优点

1. [列出做得好的地方]
2. ...

---

## ⚠️ 需改进项

### 严重问题（必须修复）

| 文件 | 行号 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| `js/main.js` | 45 | 存在 XSS 风险 | 使用 `textContent` 替代 `innerHTML` |

### 一般问题（建议修复）

| 文件 | 行号 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| `css/main.css` | 120 | 魔法数字 | 提取为 CSS 变量 |

### 优化建议（可选）

| 文件 | 问题描述 | 建议 |
|------|----------|------|
| `index.html` | 图片未懒加载 | 添加 `loading="lazy"` |

---

## 📈 性能指标

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| Lighthouse 性能 | 85 | ≥90 | ⚠️ |
| Lighthouse 无障碍 | 92 | ≥90 | ✅ |
| Lighthouse 最佳实践 | 88 | ≥90 | ⚠️ |
| Lighthouse SEO | 95 | ≥90 | ✅ |
| 首次内容绘制 (FCP) | 1.2s | <1.5s | ✅ |
| 最大内容绘制 (LCP) | 2.8s | <2.5s | ⚠️ |

---

## 🔒 安全检查

- [x] 无 XSS 漏洞
- [ ] 无 CSRF 防护（需添加）
- [x] 依赖包无已知漏洞
- [x] 敏感信息未硬编码

---

## 📝 修改建议

### 1. [问题标题]

**问题描述：**  
[详细描述]

**修复建议：**
```javascript
// 修复前
element.innerHTML = userInput;

// 修复后
element.textContent = userInput;
```

---

## ✅ 审查结论

- [ ] 通过，可合并到 main
- [x] 需修改后重新审查
- [ ] 拒绝，需重大重构

**下次审查日期：** YYYY-MM-DD

---

*白泽 📖 · 羲和实验室代码审查系统*
```

---

## 代码规范

### 命名规范

```javascript
// ✅ 好的命名
const MAX_RETRY_COUNT = 3;
function fetchUserData() {}
const isUserLoggedIn = true;
const userList = [];

// ❌ 差的命名
const n = 3;
function fetch() {}
const flag = true;
const arr = [];
```

### 注释规范

```javascript
/**
 * 计算羲和实验室的每日访问量
 * @param {string} date - 日期字符串 (YYYY-MM-DD)
 * @returns {Promise<number>} 访问量数字
 * @throws {Error} 当日期格式无效时
 */
async function calculateDailyVisits(date) {
  // 使用 Intersection Observer 优化滚动性能
  // 参考：https://developer.mozilla.org/zh-CN/docs/Web/API/IntersectionObserver
  const observer = new IntersectionObserver(...);
}
```

### 文件组织

```
xihe-zone/
├── index.html          # 主页面
├── css/
│   ├── main.css        # 主样式文件
│   └── components/     # 组件样式
│       ├── header.css
│       └── cards.css
├── js/
│   ├── main.js         # 入口文件
│   ├── animations.js   # 动画模块
│   ├── components.js   # 组件模块
│   └── utils/          # 工具函数
│       ├── debounce.js
│       └── helpers.js
└── public/             # 静态资源
    ├── logo.svg
    └── images/
```

---

## 自动化检查

### 已配置工具

| 工具 | 用途 | 命令 |
|------|------|------|
| ESLint | JavaScript 代码检查 | `npm run lint` |
| Prettier | 代码格式化 | `npm run format` |
| Stylelint | CSS 代码检查 | `npm run lint:css` |
| Lighthouse | 性能/无障碍审计 | `npm run audit` |

### 配置示例

详见：
- `.eslintrc.json` - ESLint 配置
- `.stylelintrc.json` - Stylelint 配置
- `.prettierrc` - Prettier 配置
- `.github/workflows/ci.yml` - CI/CD 配置

---

## 快速开始

### 本地开发检查

```bash
# 安装依赖
npm install

# 运行代码检查
npm run lint

# 格式化代码
npm run format

# 开发模式
npm run dev

# 构建生产版本
npm run build
```

### 提交前检查清单

- [ ] `npm run lint` 无错误
- [ ] `npm run format` 已执行
- [ ] 代码审查清单已核对
- [ ] 测试通过（如有）
- [ ] Git 提交信息规范

---

*最后更新：2026-02-28*  
*白泽 📖 · 羲和实验室代码审查系统*
