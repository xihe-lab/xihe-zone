# 代码审查快速开始指南

> 📖 白泽守护 · 5 分钟上手代码审查流程

---

## 🚀 快速开始

### 1. 安装开发依赖

```bash
cd xihe-zone
npm install
```

这将安装：
- ✅ ESLint - JavaScript 代码检查
- ✅ Stylelint - CSS 代码检查
- ✅ Prettier - 代码格式化
- ✅ Husky - Git Hooks
- ✅ lint-staged - 提交前自动检查

### 2. 配置 Git Hooks（可选但推荐）

```bash
# 初始化 Husky
npx husky init

# 编辑 .husky/pre-commit，添加：
npm run lint
npm run lint:css
npm run format:check
```

### 3. 开发工作流程

```bash
# 1. 创建新分支
git checkout -b feature/your-feature

# 2. 编写代码...

# 3. 提交前自动检查（lint-staged 会运行）
git add .
git commit -m "feat: 添加新功能"

# 4. 推送到 dev 分支
git push origin feature/your-feature

# 5. 创建 Pull Request 到 dev 分支
```

### 4. 手动运行检查

```bash
# 检查 JavaScript
npm run lint

# 自动修复 JavaScript 问题
npm run lint:fix

# 检查 CSS
npm run lint:css

# 自动修复 CSS 问题
npm run lint:css:fix

# 格式化所有文件
npm run format

# 检查格式化（不修改）
npm run format:check
```

---

## 📋 提交前检查清单

在提交代码到 dev 分支前，请确认：

- [ ] `npm run lint` 无错误
- [ ] `npm run lint:css` 无错误
- [ ] `npm run format:check` 通过
- [ ] 代码审查清单已核对（参考 CODE_REVIEW.md）
- [ ] Git 提交信息规范（动词开头，简洁描述）
- [ ] 已测试功能正常

---

## 🎯 代码审查流程

### 墨子 ⚙️ 提交流程

```
1. 本地开发 → 2. 运行检查 → 3. 提交到 dev → 4. 创建 PR
```

### 白泽 📖 审查流程

```
1. 自动检查（GitHub Actions） → 2. 人工审查 → 3. 输出报告 → 4. 反馈修改
```

### 合并流程

```
1. 修复问题 → 2. 重新审查 → 3. 审核通过 → 4. 合并到 main → 5. 自动部署
```

---

## 📖 审查文档

| 文档 | 用途 |
|------|------|
| `CODE_REVIEW.md` | 审查流程、清单、规范 |
| `CODE_REVIEW_REPORT.md` | 审查报告模板和实例 |
| `CODE_REVIEW_QUICKSTART.md` | 本文件，快速上手指南 |

---

## 🛠️ 工具配置

### ESLint 规则要点

- 2 空格缩进
- 单引号
- 语句末尾加分号
- 使用 `const`/`let`，禁止 `var`
- 函数最大行数建议 50 行
- 行宽限制 120 字符

### Stylelint 规则要点

- 2 空格缩进
- 单引号
- 禁止 `!important`
- 禁止 ID 选择器
- 属性按字母顺序排列
- 最多 3 层嵌套

### Prettier 配置要点

- 行宽 100 字符
- 2 空格缩进
- 单引号
- 末尾逗号（多行时）
- 分号：是

---

## ⚠️ 常见问题

### Q: ESLint 报错太多怎么办？

A: 先运行 `npm run lint:fix` 自动修复格式问题，然后手动修复逻辑问题。

### Q: 某个规则不合理，可以禁用吗？

A: 可以。在 `.eslintrc.json` 或 `.stylelintrc.json` 中调整规则，或在该行上方添加注释：

```javascript
// eslint-disable-next-line no-unused-vars
const _unused = 'intentional';
```

### Q: 如何跳过 Git Hooks？

A: 使用 `--no-verify` 参数（不推荐）：

```bash
git commit -m "feat: xxx" --no-verify
```

### Q: CI/CD 失败怎么办？

A: 查看 GitHub Actions 日志，根据错误信息修复。通常是 lint 或 build 失败。

---

## 📞 联系白泽

如有疑问，请在审查报告中提出，白泽 📖 会及时解答。

---

*白泽 📖 · 羲和实验室代码审查系统*  
*最后更新：2026-02-28*
