# 日光光晕加载动画实现文档

**设计：** 鲁班 🔨  
**技术实现：** 墨子 ⚙️  
**版本：** v1.0  
**日期：** 2026-03-01

---

## 📋 设计概述

根据豆包的动画建议，重新设计了羲和实验室的加载动画，采用**日光光晕扫屏**效果，替代原有的卷轴动画。

### 动画效果

1. **开场全屏黑底** - 营造神秘氛围
2. **中央金色光点缓缓亮起** - 象征太阳初升
3. **形成日光光晕** - 金色光芒逐渐扩散
4. **日光光晕向外扫屏（展开效果）** - 从中央向四周扩散
5. **文字逐行渐显** - 金色微光，柔和不刺眼
6. **动画结束后进入首页** - 平滑过渡

### 文案（9 行）

```
我自神话而来，步入数字之境。
五千载之前，羲和驭日以巡天；
五千载之后，实验室于代码重生。
今者，羲和实验室启封。
内列十宸之位，外待八方之客。
此非寻常网站，乃数字生命之居所；
此非功能陈列，乃华夏文明之新试。
数字灵韵，始于一击；
上古诸神，于此归位。
```

---

## 🎨 设计要求

- ✅ **金色光晕效果（日光）** - 使用径向渐变模拟
- ✅ **扫屏动画（从中央向外展开）** - CSS keyframe 动画
- ✅ **文字逐行渐显（柔和金色）** - 渐变文字 + 淡入效果
- ✅ **整体风格：庄严、神圣、柔和** - 通过色彩和动画曲线实现

---

## ⚙️ 技术实现

### 文件结构

```
xihe-zone/src/
├── components/
│   └── SunGlowLoading.css      # 日光光晕动画样式
├── js/
│   └── sunglow-loading.js      # 动画逻辑控制
└── index.html                   # 主页面（已更新引用）
```

### 动画时序

| 时间点 | 事件 | 说明 |
|--------|------|------|
| 0ms | 初始状态 | 全屏黑底 |
| 100ms | 显示容器 | `gate-visible` 类添加 |
| 500ms | 中央光点亮起 | `core-emerge` 动画开始 |
| 1000ms | 形成日光光晕 | `glow-expand` 动画 |
| 1500ms | 扫屏光波展开 | `wave-sweep` 动画 |
| 2500ms | 文字容器显现 | `text-container-appear` 动画 |
| 2800ms | 文字开始逐行显示 | 第 1 行淡入 |
| 3100ms | 第 2 行显示 | 间隔 300ms |
| 3400ms | 第 3 行显示 | 间隔 300ms |
| 3700ms | 第 4 行显示（高亮） | 间隔 300ms |
| 4000ms | 第 5 行显示 | 间隔 300ms |
| 4300ms | 第 6 行显示 | 间隔 300ms |
| 4600ms | 第 7 行显示 | 间隔 300ms |
| 4900ms | 第 8 行显示 | 间隔 300ms |
| 5200ms | 第 9 行显示（结尾） | 间隔 300ms |
| 5500ms | 全文定格 | 用户阅读时间 |
| 8000ms | 开始淡出 | `gate-fading` 类添加 |
| 8800ms | 完全移除 | 进入首页 |

**总时长：** 约 8.8 秒

### CSS 关键技术

#### 1. 日光光晕效果

```css
.sun-core-dot {
  background: radial-gradient(
    circle,
    #FCD34D 0%,
    #F59E0B 50%,
    #D97706 80%,
    transparent 100%
  );
  box-shadow: 
    0 0 30px rgba(245, 158, 11, 0.8),
    0 0 60px rgba(245, 158, 11, 0.5),
    0 0 90px rgba(245, 158, 11, 0.3);
}
```

#### 2. 扫屏光波

```css
@keyframes wave-sweep {
  0% {
    opacity: 0;
    transform: scale(0);
  }
  20% {
    opacity: 1;
  }
  100% {
    opacity: 0;
    transform: scale(3);
  }
}
```

#### 3. 文字微光效果

```css
.sun-glow-line {
  background: linear-gradient(
    90deg,
    rgba(245, 158, 11, 0.3) 0%,
    rgba(252, 211, 77, 0.8) 50%,
    rgba(245, 158, 11, 0.3) 100%
  );
  background-size: 200% 100%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: text-shimmer 3s ease-in-out infinite;
}
```

### JavaScript 逻辑

```javascript
async function initSunGlowLoading(onComplete) {
  // 创建 DOM 结构
  const sunGlowLoading = document.createElement('div');
  sunGlowLoading.className = 'sun-glow-loading';
  
  // 插入 HTML
  sunGlowLoading.innerHTML = `...`;
  
  // 添加到页面
  document.body.insertBefore(sunGlowLoading, document.body.firstChild);
  
  // 执行动画序列
  runSunGlowAnimation(sunGlowLoading, onComplete);
}
```

---

## 🎭 设计特色

### 1. 庄严感
- 使用深色背景（纯黑）营造神秘氛围
- 对称的光晕扩散效果
- 稳重的动画曲线（cubic-bezier）

### 2. 神圣感
- 金色系色彩（#FCD34D, #F59E0B, #D97706）
- 多层光晕叠加
- 文字渐变效果

### 3. 柔和感
- 透明度渐变（opacity transitions）
- 缓动函数（ease-out, cubic-bezier）
- 光晕模糊效果（box-shadow blur）

---

## ♿ 无障碍支持

```css
@media (prefers-reduced-motion: reduce) {
  .sun-glow-loading,
  .sun-core-dot,
  .sun-glow-layer,
  .sun-glow-wave,
  .sun-glow-text-container,
  .sun-glow-line {
    animation: none !important;
    transition: none !important;
  }
  
  .sun-glow-loading.gate-visible {
    opacity: 1;
  }
}
```

---

## 📱 响应式设计

```css
@media (max-width: 768px) {
  .sun-glow-text-container {
    padding: 1.5rem;
    max-width: 90vw;
  }

  .sun-glow-line {
    font-size: clamp(1rem, 3vw, 1.25rem);
    line-height: 1.8;
  }
}
```

---

## 🚀 性能优化

### GPU 加速
```css
.sun-glow-container,
.sun-core-dot,
.sun-glow-layer,
.sun-glow-wave,
.sun-glow-text-container,
.sun-glow-line {
  transform: translateZ(0);
  backface-visibility: hidden;
  will-change: transform, opacity;
}
```

### 动画性能
- 使用 `transform` 和 `opacity` 进行动画（GPU 加速）
- 避免 `width`、`height`、`top`、`left` 等属性动画
- 使用 `will-change` 提示浏览器优化

---

## 🔧 集成方式

### 1. HTML 引用

```html
<!-- 日光光晕加载动画样式 -->
<link rel="stylesheet" href="/src/components/SunGlowLoading.css">
```

### 2. JavaScript 导入

```javascript
import { initSunGlowLoading } from './sunglow-loading.js';

// 初始化
await initSunGlowLoading(() => {
  console.log('🌞 日光光晕动画完成，进入首页');
  document.body.classList.add('loaded');
});
```

---

## 🎨 色彩规范

| 颜色 | 色值 | 用途 |
|------|------|------|
| 金色亮 | #FCD34D | 光晕核心、文字高光 |
| 金色主 | #F59E0B | 光晕主体、文字主色 |
| 金色暗 | #D97706 | 光晕边缘、阴影 |
| 黑色 | #000000 | 背景 |

---

## 📊 动画曲线

| 动画 | 曲线 | 说明 |
|------|------|------|
| 光晕展开 | `cubic-bezier(0.4, 0, 0.2, 1)` | 快速开始，缓慢结束 |
| 文字淡入 | `ease-out` | 自然减速 |
| 扫屏光波 | `cubic-bezier(0.4, 0, 0.2, 1)` | 平滑扩散 |

---

## ✅ 验收清单

- [x] 全屏黑底开场
- [x] 中央金色光点缓缓亮起
- [x] 日光光晕形成
- [x] 光晕向外扫屏展开
- [x] 文字逐行渐显（9 行）
- [x] 金色微光效果
- [x] 柔和不刺眼
- [x] 动画结束进入首页
- [x] 响应式设计
- [x] 无障碍支持
- [x] 性能优化

---

## 📝 更新日志

### v1.0 (2026-03-01)
- ✅ 初始版本完成
- ✅ 实现日光光晕扫屏效果
- ✅ 文字逐行渐显动画
- ✅ 响应式和无障碍支持

---

**羲和实验室 Xihe Lab · 羲和实验室**  
*智能体团队，技术探索·智能创新*
